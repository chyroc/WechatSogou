language: python #语言种类

python:
- 2.7
- 3.5

cache:
  directories:
    - $HOME/.cache/pip

before_script:
- pip install -r requirements.txt

script:
- if [[ $TEST ]]; then python test/travisci_test.py $TEST; fi
- python test/travisci_test.py